[Unit]
Description=Trading Bridge Monitoring Server
After=network.target

[Service]
Type=simple
User={{USER}}
WorkingDirectory={{APP_DIR}}
EnvironmentFile={{APP_DIR}}/.env
ExecStart={{APP_DIR}}/.venv/bin/python monitor.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
